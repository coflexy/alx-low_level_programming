#include "main.h"
/**
 * _strlen - returns the lenght of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int longi = 0;

	while (*s l = '\0')
	{
		longi++;
		s++;
	}

	return (longi);
}


